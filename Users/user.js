class User {
 constructor (id, firstName, lastNname, email, age, image) {
 this.id = id;
 this.firstName = firstName;
 this.lastName = lastNname;
 this.email = email;
 this.age = age;
 this.image = image;
 }
}